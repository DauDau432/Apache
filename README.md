# Apache
### Cài đặt Apache
Trên ubuntu
```
sudo apt install apache2 -y
```
Trên centos
```
sudo yum install httpd -y
```
### Quản lý Apache
Trên ubuntu
```
sudo apt install apache2 -y
```
Trên centos
```
sudo yum install httpd -y
